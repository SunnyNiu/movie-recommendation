// const environment = process.env.NODE_ENV || 'development'
// const config = require('./knexfile')[environment]
// const connection = require('knex')(config)

// module.exports = {
//   getMovie: getMovie
// }

// function getMovie (id, db = connection) {
//   return db('movies')
//     .where('movies.id', Number(id))
//     .first()
// }
